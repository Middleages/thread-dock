# Repository rules

- Change only the assigned paths for the Task.
- Keep production credentials out of the repository and command output.
- Write commit messages with Korean change context.
- Preserve every Worktree; an operator decides any forced deletion.
- End with: Task, changed paths, checks, commit SHA, remaining risk, and blocker status.
